# DXN Core Initialization Flow (`dxn-core`)

This document describes how `dxn-core` initializes itself from `config.json` and interacts with the provisioning metadata created by `dxn-setup`.

---

## Overview

`dxn-core` performs **one-time initialization** from `config.json` and then treats its database and vault as the primary source of truth.

- First run after provisioning:
  - Reads `config.json` and `.dxn-setup-lock.json`.
  - Initializes database schema, functions, routes, and integrations.
  - Verifies vault key using the shared secret and KDF metadata.
  - Writes `.dxn-core-state.json`.

- Subsequent runs:
  - Do **not** re-create schema or seed data.
  - Validate configuration and vault state.
  - Start the server normally.

---

## Startup Flow

### 1. Locate configuration and metadata

1. Resolve working directory (normally `dxn-core`).
2. Check for:

   - `config.json`
   - `.dxn-setup-lock.json`
   - `.dxn-core-state.json` (may not exist on first run)

3. If `config.json` is missing:

   - Fail:  
     > config.json not found. This file must be generated by dxn-setup.

4. If `.dxn-setup-lock.json` is missing:

   - Fail:  
     > DXN core is not provisioned. Run dxn-setup before starting the server.

---

### 2. Vault key verification

1. Read:

   - Vault path,
   - KDF parameters,
   - salt,
   - `key_fingerprint`

   from `.dxn-setup-lock.json`.

2. Obtain shared secret at runtime (e.g. from `DXN_CORE_SECRET` env var).

3. Derive key using same KDF parameters.

4. Compute fingerprint as `HMAC-SHA256(derived_key, "DXN_VAULT")`.
5. Compare with stored `key_fingerprint`:

   - If mismatch → fail:
     > Invalid shared secret for this server instance.

6. Open the vault file using the derived key.

---

### 3. First-run initialization

If `.dxn-core-state.json` is **missing** or `initialized` is `false`:

1. Compute `config_checksum = sha256(config.json)`.

2. Initialize:

   - **Database schema** from config’s data models.
   - **Functions** and **integrations** registry from config.
   - **Server routes** mapping from config.

3. Apply any migrations required for version `db_migration_version = 1`.

4. Write `.dxn-core-state.json`:

   ```json
   {
     "initialized": true,
     "instance_id": "<instance id from .dxn-setup-lock.json>",
     "initialized_at": "<timestamp>",
     "config_checksum": "sha256:...",
     "db_migration_version": 1
   }
   ```

5. Continue to normal server startup (bind to host/port, etc.).

---

### 4. Subsequent runs

If `.dxn-core-state.json` indicates `initialized: true`:

1. Compute `config_checksum` for current `config.json`.
2. Compare with `config_checksum` stored in `.dxn-core-state.json`:

   - If **equal**:
     - Skip schema/seed steps.
     - Proceed to open DB and start server.

   - If **different**:
     - Option A (warning mode):

       - Log warning:
         > config.json has changed since initialization. These changes will not be automatically applied; use migration tools.

       - Continue starting the server using existing DB/vault.

     - Option B (strict mode):

       - Fail:
         > config.json has changed since initialization. Please run dxn-migrate or restore the original config.

3. Verify vault as in section 2 and start normal operation.

---

## Vault Ready Indicator

A server instance is considered **vault ready** when:

1. `.dxn-setup-lock.json` exists and is valid.
2. The shared secret provided at runtime produces the expected `key_fingerprint`.
3. The vault file can be opened with the derived key.

You may optionally write a simple `vault.ready` file after a successful open to simplify monitoring and health checks.

---

## Updating Configuration After Initialization

After initial provisioning:

- **config.json** is treated as a **record of the initial template**, not as a live source of truth.
- All runtime changes (data models, functions, integrations, routes) should be applied via:
  - admin APIs, or
  - explicit migration tools that update the database and vault.

If you need to share a server’s configuration as a template:

1. Export from the live DB/vault to a new `config.template.json`.
2. Use that template with `dxn-setup` to provision **new** instances.

Existing instances are never re-initialized from `config.json`.

