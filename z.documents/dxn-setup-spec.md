# DXN Setup Spec (`dxn-setup` & `dxn-core`)

## Overview

`dxn-setup` is a **compiled, one-time provisioning CLI** that:

1. Derives a vault master key from a shared secret using Argon2/Balloon.
2. Preloads vault key/value pairs from a file.
3. Generates the final `dxn-core/config.json` from a template + settings.
4. Writes a provisioning lock/metadata file to prevent re‑initialization.

`dxn-core`:

- Uses the provisioning metadata to verify the vault key.
- Runs a **one-time** initialization from `config.json`.
- Treats DB + vault as the source of truth thereafter.

---

## Directory Layout

- `dxn-core/`
  - `config.json` – final runtime config (generated by `dxn-setup`)
  - `vault.db` – encrypted vault storage
  - `.dxn-setup-lock.json` – provisioning metadata
  - `.dxn-core-state.json` – core initialization state

- `dxn-setup/`
  - Rust crate producing a single executable: `dxn-setup`

---

## `dxn-setup` CLI

### Usage

```bash
dxn-setup \
  --project-root /path/to/project \
  --config-template ./config.template.json \
  --settings-file ./settings.json \
  --secrets-file ./secrets.txt
```

### Arguments

- `--project-root <path>` (**required**)  
  Root of the DXN project; `dxn-core` is expected at `<project-root>/dxn-core`.

- `--config-template <path>` (**required**)  
  Template config describing data models, functions, routes, etc. Used to generate `dxn-core/config.json`.

- `--settings-file <path>` (optional but recommended)  
  JSON with environment-specific settings (host, ports, base URL, etc.) merged into the template.

- `--secrets-file <path>` (**required**)  
  Text (or JSON) file with vault key/value pairs to preload, e.g.:

  ```text
  profile.email=admin@example.com
  profile.phone=+64 21 000 0000
  wallet.address=0x123...
  ```

- `--kdf-profile <name>` (optional; default `standard`)  
  Selects Argon2/Balloon parameters (memory/iterations/parallelism).

- `--non-interactive` (optional)  
  If present, read:
  - `DXN_SETUP_USERNAME`
  - `DXN_SETUP_SECRET`  
  from environment instead of prompting.

### Interactive Prompts

If `--non-interactive` is **not** set:

1. **Username** – audit only, stored in metadata.
2. **Shared secret** – read without echo; used to derive the vault key.

No secret is logged or stored in plaintext.

### Exit Codes

- `0` – success.
- `10` – `.dxn-setup-lock.json` already exists (instance already provisioned).
- `11` – invalid/missing `--project-root`.
- `12` – template/settings file error.
- `13` – secrets file error.
- `14` – KDF/vault error.
- `15` – failed to write config or lock.

---

## Files Written by `dxn-setup`

### 1. `dxn-core/config.json`

Generated by:

- Loading `config.template.json`.
- Merging values from `settings.json` (if present).
- Optional validation (required fields, no conflicting routes, etc.).

After initialization, `dxn-core` does **not** re‑apply changes from `config.json` automatically.

### 2. `dxn-core/vault.db`

Encrypted vault containing secrets from `--secrets-file`.

- Master key derived via Argon2/Balloon:
  - Random salt.
  - Tunable memory/iterations/parallelism.
- On disk you store:
  - KDF params + salt.
  - A **key fingerprint**: `HMAC-SHA256(derived_key, "DXN_VAULT")`.
- You **never** store the plaintext shared secret or raw key.

### 3. `dxn-core/.dxn-setup-lock.json`

Provisioning metadata, e.g.:

```json
{
  "instance_id": "e7b9bb65-3c67-4ef7-9c2a-9f3acb5d3e21",
  "project_root": "/abs/path/to/project",
  "created_at": "2026-01-27T12:34:56Z",
  "created_by": "admin@example.com",
  "config_checksum": "sha256:abc123...",
  "vault": {
    "path": "./dxn-core/vault.db",
    "kdf": {
      "algorithm": "argon2id",
      "profile": "standard",
      "salt": "base64-encoded",
      "memory_mb": 256,
      "iterations": 5,
      "parallelism": 1
    },
    "key_fingerprint": "hmac-sha256(derived_key, 'DXN_VAULT')"
  }
}
```

Behavior:

- If this file exists when `dxn-setup` starts → exit with code `10` and message:
  > This DXN instance has already been provisioned. To create a new server, use a new project directory.

There is **no reset** mode: provisioning is one‑time per instance.

---

## `dxn-core` Initialization

### Startup Checks

On start, `dxn-core` must:

1. Ensure `config.json` exists. If missing, fail:
   > config.json not found. This file must be generated by dxn-setup.

2. Ensure `.dxn-setup-lock.json` exists. If missing, fail:
   > DXN core is not provisioned. Run dxn-setup before starting the server.

3. Read vault metadata from `.dxn-setup-lock.json`.

4. Obtain shared secret at runtime (e.g. `DXN_CORE_SECRET` env var).

5. Derive key using KDF parameters + salt, compute fingerprint, and compare with stored `key_fingerprint`:
   - If mismatch → fail:
     > Invalid shared secret for this server instance.

6. Open `vault.db` with the derived key.

### One-time Schema & Seed – `.dxn-core-state.json`

On **first successful run**:

1. Compute `config_checksum = sha256(config.json)`.
2. From `config.json`:
   - Create DB schemas for data models.
   - Register functions, integrations, routes.
3. Apply initial migrations (e.g. `db_migration_version = 1`).
4. Write:

```json
{
  "initialized": true,
  "instance_id": "<from .dxn-setup-lock.json>",
  "initialized_at": "<timestamp>",
  "config_checksum": "sha256:...",
  "db_migration_version": 1
}
```

Subsequent runs:

1. Read `.dxn-core-state.json`; if `initialized != true` → treat as error or first-run case.
2. Compute new `config_checksum` and compare:
   - If equal → start normally.
   - If different:
     - Option (warn):
       > config.json has changed since initialization. Changes will not be auto-applied; use migration tools.
     - Option (strict):
       > config.json has changed since initialization. Please run dxn-migrate or restore the original config.

No automatic re-seeding from `config.json` after the first run.

---

## Vault Ready Indicator

A DXN instance is “vault ready” when:

1. `.dxn-setup-lock.json` exists and is valid.
2. The runtime shared secret yields the correct `key_fingerprint`.
3. `vault.db` opens successfully with the derived key.

Optionally, `dxn-core` may write a simple `vault.ready` file after a successful open for health checks.

---

## Text Flows

See:

- `dxn-setup-flow.md` – step-by-step provisioning flow.
- `dxn-core-init-flow.md` – step-by-step server initialization flow.

These flows describe the same behavior in narrative form for operators and contributors.

