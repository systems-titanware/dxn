[package]
name = "dxn-functions"
version = "0.1.0"
edition = "2024"

[dependencies]
wasmtime = { version="40.0.2", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"

[lib]
crate-type = ["cdylib", "rlib"]
