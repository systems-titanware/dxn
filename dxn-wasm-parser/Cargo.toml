[package]
name = "dxn-wasm-parser"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dxn_shared = { path = "../dxn-shared" }
pulldown-cmark = "0.9"

[lib]
crate-type = ["cdylib", "rlib"]
