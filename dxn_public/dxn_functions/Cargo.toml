[package]
name = "dxn_functions"
version = "0.1.0"
edition = "2024"

[dependencies]
wasmtime = { version="39.0.1", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"

[lib]
crate-type = ["cdylib", "rlib"]
