[package]
name = "dxn-native-wallet"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
dxn_shared = { path = "../dxn-shared" }
dxn_core = { path = "../dxn-core" }

[lib]
crate-type = ["cdylib", "rlib"]

